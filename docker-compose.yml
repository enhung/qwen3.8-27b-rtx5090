# docker-compose.yml
#
# Model selection comes from .env (see .env.example); unset keys keep the
# defaults, which serve the stock Qwen3.8-27B NVFP4 model:
#   MODEL_SUBDIR        weights dir under ./models/ (setup.sh downloads into it)
#   SERVED_MODEL_NAME   value for --served-model-name
#   VLLM_API_KEY        bearer key, empty = no auth
#   VLLM_HOST_PORT      host port for the direct vLLM API (default 8020)
#   GATEWAY_HOST_PORT   host port for the caddy gateway (default 8030)
#
# Performance flags (vllm serve command below; the mtp override mirrors the
# command with its own built-in defaults) are .env-overridable:
#   VLLM_GPU_MEMORY_UTILIZATION  --gpu-memory-utilization (this file: 0.95)
#   VLLM_MAX_NUM_SEQS           --max-num-seqs (this file: 16)
# docker-compose.mtp.yml ships the dedicated-card ideal-usage tuning
# (3 / 0.965, the shipped full-MTP tuning, ≈120–170 tok/s single-stream
# decode) and assumes a dedicated card (nothing else on the RTX 5090, no
# display on it) — see .env.example and the README section "Usage modes".
#
# All services: restart: "unless-stopped" (crash-restart, manual stop stays
# stopped).
#
# This base file is the light setup: no MTP. MTP heavy usage = run
# docker-compose.mtp.yml as an override file.
#
services:
  vllm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${CONTAINER_NAME:-vllm}
    restart: "unless-stopped"
    # Host port 8020 (VLLM_HOST_PORT) is the direct full vLLM API
    # (OpenAI-compatible /v1/* plus /metrics, /health and /docs; bearer
    # auth = VLLM_API_KEY on the API routes; open when the key is empty).
    # The caddy gateway on 8030 and Prometheus also reach it on the docker
    # network at vllm:8000.
    volumes:
      # Model weights, downloaded by setup.sh, mounted read-only
      - ./models/${MODEL_SUBDIR:-qwen3.8-27b-nvfp4}:/models/${MODEL_SUBDIR:-qwen3.8-27b-nvfp4}:ro
    environment:
      # CUDA and optimizer tuning
      - CUDA_VISIBLE_DEVICES=0
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:512
      - VLLM_FLOAT32_MATMUL_PRECISION=high
      - VLLM_NO_USAGE_STATS=1
      # Quite high RAM demand for building graphs - 64 GB can be not enough - adjust for our system 
      - MAX_JOBS=4
      - NVCC_THREADS=4
      # Secrets — loaded from .env (see .env.example), leave empty to disable auth
      - VLLM_API_KEY=${VLLM_API_KEY:-}
    shm_size: "16gb"
    ports:
      # VLLM_HOST_PORT from .env (default 8020); container port stays 8000
      - "${VLLM_HOST_PORT:-8020}:8000"
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1 # single RTX5090
              capabilities: [gpu]
    command:
      - --model
      - /models/${MODEL_SUBDIR:-qwen3.8-27b-nvfp4}
      - --served-model-name
      - ${SERVED_MODEL_NAME:-qwen3.8-27b}
      - --quantization
      - modelopt
      - --kv-cache-dtype
      - fp8
      - --tensor-parallel-size
      - "1"
      - --trust-remote-code
      - --max-model-len
      - "262144"
      - --max-num-seqs
      - ${VLLM_MAX_NUM_SEQS:-16}
      - --gpu-memory-utilization
      - ${VLLM_GPU_MEMORY_UTILIZATION:-0.95}
      - --reasoning-parser
      - qwen3
      - --enable-prefix-caching
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_xml
      - --host
      - 0.0.0.0
      - --port
      - "8000"

  # Gateway on host port 8030 (GATEWAY_HOST_PORT; the vLLM API itself is
  # served directly on 8020, VLLM_HOST_PORT). Expose 8030 externally to
  # host the OpenAI-compatible endpoint — 8020 stays as the direct
  # endpoint. Routed paths:
  #   /v1, /v1/*    -> vLLM — HTTP Basic auth (METRICS_USER / METRICS_HASH
  #                    from .env; caddy translates it into the vLLM bearer
  #                    token, VLLM_API_KEY, for the upstream call), OR the
  #                    vLLM key itself: a client sending
  #                    Authorization: Bearer <VLLM_API_KEY> passes through
  #                    untouched (route ordered before the Basic route), so
  #                    OpenAI-compatible clients can point at 8030/v1 with
  #                    the same key they use on 8020.
  #   /metrics      -> vLLM telemetry (Basic auth only)
  #   /dcgm/metrics -> dcgm-exporter /metrics (per-GPU telemetry, Basic
  #                    auth only)
  # Everything else falls through to caddy's empty default response.
  caddy:
    # fail-closed: caddy refuses to start if METRICS_HASH is unset (basic
    # auth cannot work without it) — fill .env first on a fresh stack
    image: caddy:2.11.4-alpine
    container_name: caddy-vllm
    restart: "unless-stopped"
    ports:
      # GATEWAY_HOST_PORT from .env (default 8030); container port stays 8030
      - "${GATEWAY_HOST_PORT:-8030}:8030"
    environment:
      - METRICS_USER=${METRICS_USER:-metrics}
      - METRICS_HASH=${METRICS_HASH:-}
      - VLLM_API_KEY=${VLLM_API_KEY:-}
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - vllm
      - dcgm-exporter

  # Scrapes vllm:8000/metrics and dcgm-exporter:9400 (see
  # prometheus/prometheus.yml).
  # UI bound to 127.0.0.1:9090 — no public exposure, access locally or via
  # SSH tunnel. 30 days of data kept in the prometheus_data volume.
  prometheus:
    image: prom/prometheus:v3.13.2
    container_name: prometheus
    restart: "unless-stopped"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=30d
      - --web.enable-lifecycle
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    depends_on:
      - vllm
      - dcgm-exporter

  # vLLM dashboard is auto-provisioned from ./grafana/dashboards/ (folder
  # "vllm"). Login: admin / GRAFANA_ADMIN_PASSWORD from .env.
  grafana:
    image: grafana/grafana:11.1.4
    container_name: grafana
    restart: "unless-stopped"
    ports:
      - "0.0.0.0:3000:3000"
    environment:
      # note: applied only on first Grafana DB init (fresh grafana_data
      # volume); rotate the admin password later via the Grafana UI
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-}
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus

  # Per-GPU metrics (utilization, memory, power, temps). Exposes
  # DCGM_FI_DEV_* names on the docker network only (Prometheus scrapes
  # dcgm-exporter:9400); external access goes through the caddy gateway on
  # 8030 (/dcgm/metrics) — no host port of its own.
  # See prometheus/prometheus.yml (job "dcgm").
  #
  # Built from a public base image + pypi (dcgm-exporter/), so no nvcr.io
  # (NGC registry) login is needed. The exporter only needs the NVML
  # driver library, which the NVIDIA container toolkit injects into any
  # container that reserves the GPU.
  # Prefer the official image? Replace the `build:` block with:
  #   image: nvcr.io/nvidia/dcgm-exporter:3.3.9-3.6.0-ubuntu22.04
  dcgm-exporter:
    build:
      context: dcgm-exporter
    container_name: dcgm-exporter
    restart: "unless-stopped"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  caddy_data:
  caddy_config:
  prometheus_data:
  grafana_data:
